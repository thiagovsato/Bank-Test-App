@model IEnumerable<Teste7.Models.Transferencia>

@{
    ViewBag.Title = "Transferencias";
}

<h2>Transferencias</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientePagador.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClienteRecebedor.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientePagador.NomeCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClienteRecebedor.NomeCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
